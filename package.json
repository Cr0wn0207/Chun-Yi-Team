{
  "name": "chun-yi-corporate-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal company website inspired by SCSK - MERN stack",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.9.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
